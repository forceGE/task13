# Используем Node.js 20.15 в качестве базового образа
FROM node:20.15

# Устанавливаем рабочую директорию
WORKDIR /usr/src/app

# Копируем package.json и package-lock.json в рабочую директорию
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем весь исходный код в рабочую директорию
COPY . .

# Открываем порт 3000 для прослушивания внутри контейнера
EXPOSE 3000

# Запускаем приложение
CMD ["npm", "start"]
